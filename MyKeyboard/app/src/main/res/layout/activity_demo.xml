<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/demo_root"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

    <!-- 1) 검색어 입력란 -->
    <EditText
            android:id="@+id/et_search_demo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="검색어 입력"
            android:inputType="text"
            android:imeOptions="actionDone"
            android:padding="8dp" />

    <Button
            android:id="@+id/btn_do_search_demo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="검색 저장"
            android:layout_marginTop="8dp" />

    <!-- 2) 즐겨찾기 입력란 (선택) -->
    <EditText
            android:id="@+id/et_fav_demo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="즐겨찾기 추가"
            android:inputType="text"
            android:padding="8dp"
            android:layout_marginTop="16dp" />

    <Button
            android:id="@+id/btn_add_fav_demo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="즐겨찾기 저장"
            android:layout_marginTop="8dp" />

    <!-- 3) 레이블 및 RecyclerView: 최근검색어 -->
    <TextView
            android:id="@+id/tv_history_demo_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="최근 검색어"
            android:textStyle="bold"
            android:layout_marginTop="24dp" />

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_history_demo"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="#EEEEEE"
            android:layout_marginTop="8dp" />

    <!-- 4) 레이블 및 RecyclerView: 즐겨찾기 -->
    <TextView
            android:id="@+id/tv_fav_demo_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="즐겨찾기"
            android:textStyle="bold"
            android:layout_marginTop="24dp" />

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_fav_demo"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="#EEEEEE"
            android:layout_marginTop="8dp" />

</LinearLayout>